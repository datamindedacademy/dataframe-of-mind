# Reading in data

A bit less read is a bit less to process. Run both cells in the notebook `demo-reading-data.ipynb` to see the difference between reading in the data from object storage with and without using Polars' lazy API.  
